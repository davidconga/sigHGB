# macOS
.DS_Store

# Editors
.idea/
.vscode/
.zed/
.cursor/
.codex/
*.swp
.vscode-upload.json

# Logs
*.log

# Build outputs already covered by subdirectory ignores

# Backend signatures (real doctor signatures — never commit)
backend/storage/app/public/medicos/

# Legacy SQL dumps (sensitive: real patient data, hashed passwords)
legacy/
*.sql

# Built APK/AAB (uploaded to the VPS separately — too big for git)
*.apk
*.aab

# Local Claude project settings
.claude/
